name: Scheduled Automation

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  auto-task:
    runs-on: ubuntu-latest

    steps:
      - name: Run scheduled message
        run: echo "This automation runs every 5 minutes"
